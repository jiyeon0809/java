<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tobi.team3.mapper.ItemMapper">

<select id="getItemListByCategory" parameterType="com.example.tobi.team3.model.Item">
    select id,item,price,detail,seller,category, imagePath from item WHERE category = #{category} order by id;
</select>

    <select id="getItemDetail" parameterType="Long">
        select * from item WHERE id = #{id};
    </select>

    <insert id="saveItem" parameterType="com.example.tobi.team3.model.Item">
        INSERT INTO item (item, price, detail, seller, category, imagePath)
        values (#{item}, #{price}, #{detail}, #{seller}, #{category}, #{imagePath});
    </insert>

    <update id="editItem" parameterType="com.example.tobi.team3.model.Item">
        update user
        set item=#{item}, price=#{price}, category=#{category}, imagePath=#{imagePath}
        where id=#{id};
    </update>

    <select id="getItemList" parameterType="com.example.tobi.team3.model.Item">
        select * from item where seller = #{seller}
    </select>
</mapper>